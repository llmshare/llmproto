export default function Save({ }: { }) {    
    const onFileChange = () => {
 
        
 
    };
    const loadFile = () => {
        alert("TO ADD: File save function");
        return;
    };
    return(
        <button type="button" onClick={loadFile}>
        Save
      </button>)
   }
